<!-- 这个header就是特指首页的header -->

<% if(theme.index.header != null || theme.index.header == "") {%>
    <div class="index-header-line" >
        <span><%= theme.index.header %></span>
    </div>
<% } %>
<% if(theme.index.titlePosition == "left") { %>
    <div style="text-align: left;">
<% } else if(theme.index.titlePosition == "center") { %>
    <div style="text-align: center;"> 
<% } %>
        <a class="index-logo" href="<%- url_for() %>"><%= config.title %></a>
        <div style="margin-bottom: 12px;"> 
<!-- Default is HOME, Modified by xinfeng -->
            PROJECTS
            <% for (name in theme.menu) { %>
                <% if(name !== 'PROJECTS' && theme.showInMenu.includes(name)) { %>
                    &nbsp / &nbsp<a href="<%- url_for(theme.menu[name]) %>"><%= name %></a>
                <% } %>
            <% } %>
        </div>
    </div>
